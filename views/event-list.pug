doctype html
html(lang="en")
  include partials/head
  body
    main.container-fluid.py-4(style="padding-left: 4rem; padding-right: 4rem; font-size: clamp(1rem, 1.6vw, 2rem)")
      h1.mb-4 Upcoming Events
      #upcomingEvents
        p Loading...

      hr.my-5

      h1.mb-4 Past Events
      #pastEvents
        p Loading...

    script(type="module" src="/js/events-list.js")
